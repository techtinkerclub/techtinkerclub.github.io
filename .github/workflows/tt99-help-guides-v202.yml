name: Refresh Games help guides v202

on:
  push:
    branches:
      - master
    paths:
      - .github/workflows/tt99-help-guides-v202.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch help guides
        run: python3 scripts/99club/update-help-v202.py
      - name: Syntax check
        run: node --check assets/99club/games-help-guides.js
      - name: Commit changes
        run: |
          git config user.name github-actions-bot
          git config user.email github-actions-bot@users.noreply.github.com
          git rm .github/workflows/tt99-help-guides-v202.yml scripts/99club/update-help-v202.py
          git add assets/99club/games-help-guides.js _pages/99-club-games-help.md
          git commit -m "Update one-page guides for Colour Logic and Mobile Balance"
          git push origin master
