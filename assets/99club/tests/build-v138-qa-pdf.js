#!/usr/bin/env node
'use strict';
const fs=require('fs');const path=require('path');const ROOT=path.join(__dirname,'..');
require(path.join(ROOT,'games-arithmetic.js'));require(path.join(ROOT,'games-number-logic.js'));require(path.join(ROOT,'games-number-towers-v137.js'));require(path.join(ROOT,'games-number-towers-v137-unique.js'));require(path.join(ROOT,'games-number-path-v2.js'));require(path.join(ROOT,'games-sumplete.js'));const G=require(path.join(ROOT,'games-engine.js'));require(path.join(ROOT,'games-puzzle-redesign-v135-logic.js'));require(path.join(ROOT,'simple-pdf.js'));require(path.join(ROOT,'games-pdf.js'));require(path.join(ROOT,'games-pdf-v136.js'));require(path.join(ROOT,'games-pdf-v137.js'));require(path.join(ROOT,'games-pdf-v138.js'));
const PDF=globalThis.TT99GamesPDF;if(!PDF)throw new Error('PDF exporter failed to load');
const base=G.normalizeSettings({minYear:5,maxYear:6,topics:['calculation','algebra'],sheets:2,activitiesPerSheet:1,includeAnswers:true,workedExamples:'none',selectedEngines:['operationgrid','functionmachine'],personalisation:{schoolName:'99 Club Studio - visual QA',packTitle:'v1.38 Codebreaker + Function Machines',classLabel:'Pupil / teacher review',worksheetDate:'',logoDataUrl:'',logoWidth:0,logoHeight:0}});
function settingsFor(id,overrides={}){const s=JSON.parse(JSON.stringify(base));s.engineSettings[id]={...(s.engineSettings[id]||{}),...overrides};return s;}
let code=null;for(let i=0;i<80;i++){const a=G.generateActivity('operationgrid',settingsFor('operationgrid',{difficulty:'challenge'}),`qa-v138-code-${i}`);if(a&&!a.error&&(a.rows||[]).some(r=>(String(r.text||'').match(/□/g)||[]).length>=2)){code=a;break;}}
if(!code)throw new Error('Could not build multi-operation Codebreaker');
const machine=G.generateActivity('functionmachine',settingsFor('functionmachine',{difficulty:'challenge'}),'qa-v138-machine');if(!machine||machine.error)throw new Error(machine?.error||'Function Machine generation failed');
const pack={version:'1.38-QA',seed:'v138-visual-qa',settings:base,workedExamples:[],sheets:[{index:1,activities:[code]},{index:2,activities:[machine]}]};
const doc=PDF.buildDocument({pack,settings:base,topics:G.TOPICS,seed:pack.seed,kind:'both'});const outDir=path.resolve(process.cwd(),'artifacts');fs.mkdirSync(outDir,{recursive:true});const out=path.join(outDir,'v138-code-machine-qa.pdf');fs.writeFileSync(out,Buffer.from(doc.outputBytes()));console.log(`Wrote ${out} (${doc.pages.length} pages)`);
